<?xml version="1.0" encoding="utf-8"?>
<!--
  Приложение бабушки.

  Обычное имя, обычная иконка, постоянное уведомление — приложение всегда видно.
  Скрытых имён, alias-иконок и «тихого режима» здесь нет и не будет: невидимый сервис
  с доступом к чужому экрану — это stalkerware (docs/security.md §2).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- Позволяет поднять foreground-сервис из FCM-пробуждения на Android 14+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Точный будильник-сторож (авто-выдаётся при установке); при отказе есть fallback. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Яркость экрана по просьбе бабушки («ничего не вижу»). Без неё остальное управление работает. -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.NFC" />

    <!-- Нужно, чтобы гасить показ на банковских приложениях. Выдаётся вручную при настройке. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!--
      Чего мы НЕ просим намеренно: контакты, SMS, историю звонков, микрофон, геолокацию.
      CallScreeningService фильтрует звонки, не читая журнал вызовов.
    -->

    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <!--
      Видимость пакетов (Android 11+). Без этого приложение не «видит» сторонние
      приложения и не может запустить их из сценария (getLaunchIntentForPackage → null,
      в логе AppsFilter … BLOCKED). Объявляем видимыми всё, что запускается из лаунчера, —
      ровно то, что открывают сценарии; полного QUERY_ALL_PACKAGES это не требует.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".PultApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pult">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Alpha 0.4 all-in-one: выбор роли и роль «Панель» (встроенная веб-панель). -->
        <activity
            android:name=".ui.RoleActivity"
            android:exported="false"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.PanelActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name" />

        <!--
          Экран согласия. Показывается поверх блокировки и включает экран: запрос помощи
          бесполезен, если бабушка его не увидит.
        -->
        <activity
            android:name=".ui.ConsentActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Pult.Consent" />

        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:label="@string/setup_title" />

        <!-- Визуальный эмулятор экрана BankID для записи сценариев (владелец тапает
             по копии, тапы пишутся как координаты для живого BankID). -->
        <activity
            android:name=".ui.BankIdEmulatorActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:label="BankID Emulator" />

        <service
            android:name=".service.PultService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="dataSync|mediaProjection|remoteMessaging" />

        <!-- Мост автозапуска: remoteMessaging разрешён при загрузке даже на Android 15+
             (dataSync с 15 запрещён из BOOT_COMPLETED), mediaProjection — тем более. -->
        <service
            android:name=".service.BootBridgeService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="remoteMessaging" />

        <!-- FCM: главный механизм удалённого пробуждения убитого сервиса. -->
        <service
            android:name=".push.PultMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".calls.PultCallScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

<!-- V2: служба управления (.control.RemoteControlService) объявляется только во
             флейворе v2 — см. src/v2/AndroidManifest.xml. В V1 accessibility нет вовсе (§9). -->

        <receiver
            android:name=".service.ServiceHeartbeat"
            android:exported="false" />

        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <!-- Xiaomi/Huawei/HTC используют свои «быстрые» интенты загрузки. -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.huawei.android.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
