# Секрет пары не должен попасть в логи даже случайно: имена классов протокола
# сохраняем, чтобы стектрейсы читались, но ничего из ключей не логируется по коду.
-keepclassmembers class ru.pult.core.protocol.** { *; }
