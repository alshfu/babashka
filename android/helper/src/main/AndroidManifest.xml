<?xml version="1.0" encoding="utf-8"?>
<!--
  Приложение помощника.

  Обратите внимание, чего здесь НЕТ: ни оверлея, ни статистики использования,
  ни захвата экрана, ни доступа к звонкам. Помощнику всё это не нужно, и просить
  такие разрешения он не должен — иначе продукт выглядит как средство слежки.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.NFC" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <application
        android:name=".HelperApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PultHelper">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SessionActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity"
            xmlns:tools="http://schemas.android.com/tools" />

        <activity
            android:name=".ui.PairingActivity"
            android:exported="false"
            android:label="@string/pairing_title" />

        <activity
            android:name=".ui.JournalActivity"
            android:exported="false"
            android:label="@string/journal_title" />

        <service
            android:name=".PresenceService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
